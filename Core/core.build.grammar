// Core build-file grammar — vertical slice.
// A build file declares an optional package name and dependencies.
// VERSION (v1.0) is a builtin lexer class, active only when a grammar references it.

BUILD := @PACKAGE? @USING*

PACKAGE := package @IDENTIFIER:name $NEWLINE
USING   := using @IDENTIFIER:name @VERSION:version? $NEWLINE
